# ============================================
# Curely AI — Python Dependencies
# ============================================
# Top-level dependencies only (no pinned versions).
# This ensures compatibility across all platforms
# (Windows, macOS, Ubuntu/GitHub Actions).
# ============================================

# --- Web Framework ---
fastapi
uvicorn
pydantic
python-multipart
python-dotenv

# --- AI / LLM Providers ---
google-genai
openai

# --- Vector Search & Embeddings ---
faiss-cpu
sentence-transformers

# --- Document Processing ---
pdf2image
pillow
pytesseract
pypdf
PyMuPDF
python-docx

# --- Database ---
SQLAlchemy

# --- Testing (dev only, also installed explicitly in CI) ---
pytest
pytest-asyncio
httpx
flake8
